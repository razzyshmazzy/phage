source "https://rubygems.org"

# The site is built and deployed via GitHub Actions (.github/workflows/jekyll.yml),
# so we use plain Jekyll 4 — the version the site was verified against — rather
# than the github-pages gem (which pins the older Jekyll 3.9 classic-build stack).
gem "jekyll", "~> 4.3"
